import type { NotificationItem } from '@/data/notificationsSeed';

/** Starts empty — real notifications are pushed as real events happen (registration, deposits, withdrawals). */
export const adminNotificationsSeed: NotificationItem[] = [];
